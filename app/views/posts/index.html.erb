<h2>Latest posts</h2>

<%= link_to new_post_path do %>
  New post
<% end %>

<% @posts.each do |post| %>
  <div class="post">
    <p><%= post.message %></p>
    <p><%= post.created_at.strftime("%l:%M %p, %e %b %Y") %></p>
  </div>
<% end %>
